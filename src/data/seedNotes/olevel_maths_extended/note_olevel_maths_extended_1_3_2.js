export const note_olevel_maths_extended_1_3_2 = {
  pdfPath: '/notes/olevel/CIE IGCSE Maths Extended/Number/adding-and-subtracting-fractions.pdf',
  blocks: [
    {
      id: 'obj-1',
      type: 'objective',
      data: { text: 'Add and subtract fractions and mixed numbers, including those with different denominators.' }
    },
    {
      id: 'h-same-denom',
      type: 'heading',
      data: { text: 'Same Denominator', level: 2 }
    },
    {
      id: 'p-same-denom',
      type: 'paragraph',
      data: { text: 'When fractions have the <strong>same denominator</strong>, simply add or subtract the numerators and keep the denominator.' }
    },
    {
      id: 'eq-same',
      type: 'equation',
      data: { html: '<span class="nb-frac"><span class="nb-num">a</span><span class="nb-den">c</span></span> + <span class="nb-frac"><span class="nb-num">b</span><span class="nb-den">c</span></span> = <span class="nb-frac"><span class="nb-num">a + b</span><span class="nb-den">c</span></span>', caption: 'Adding fractions with the same denominator' }
    },
    {
      id: 'h-diff-denom',
      type: 'heading',
      data: { text: 'Different Denominators', level: 2 }
    },
    {
      id: 'p-diff-denom',
      type: 'paragraph',
      data: { text: 'When fractions have <strong>different denominators</strong>, you must find a <strong>common denominator</strong> first. Use the <strong>lowest common multiple</strong> (LCM) of the denominators, then convert each fraction to an equivalent fraction with this denominator.' }
    },
    {
      id: 'callout-worked-1',
      type: 'callout',
      data: { style: 'worked', title: 'Worked Example 1', text: 'Calculate <span class="nb-frac"><span class="nb-num">2</span><span class="nb-den">3</span></span> + <span class="nb-frac"><span class="nb-num">3</span><span class="nb-den">4</span></span>.\n\n<strong>Solution:</strong>\nLCM of 3 and 4 = 12.\n<span class="nb-frac"><span class="nb-num">2</span><span class="nb-den">3</span></span> = <span class="nb-frac"><span class="nb-num">8</span><span class="nb-den">12</span></span> &nbsp;&nbsp; <span class="nb-frac"><span class="nb-num">3</span><span class="nb-den">4</span></span> = <span class="nb-frac"><span class="nb-num">9</span><span class="nb-den">12</span></span>\n<span class="nb-frac"><span class="nb-num">8</span><span class="nb-den">12</span></span> + <span class="nb-frac"><span class="nb-num">9</span><span class="nb-den">12</span></span> = <span class="nb-frac"><span class="nb-num">17</span><span class="nb-den">12</span></span> = 1<span class="nb-frac"><span class="nb-num">5</span><span class="nb-den">12</span></span>' }
    },
    {
      id: 'callout-worked-2',
      type: 'callout',
      data: { style: 'worked', title: 'Worked Example 2', text: 'Calculate <span class="nb-frac"><span class="nb-num">5</span><span class="nb-den">6</span></span> − <span class="nb-frac"><span class="nb-num">1</span><span class="nb-den">4</span></span>.\n\n<strong>Solution:</strong>\nLCM of 6 and 4 = 12.\n<span class="nb-frac"><span class="nb-num">5</span><span class="nb-den">6</span></span> = <span class="nb-frac"><span class="nb-num">10</span><span class="nb-den">12</span></span> &nbsp;&nbsp; <span class="nb-frac"><span class="nb-num">1</span><span class="nb-den">4</span></span> = <span class="nb-frac"><span class="nb-num">3</span><span class="nb-den">12</span></span>\n<span class="nb-frac"><span class="nb-num">10</span><span class="nb-den">12</span></span> − <span class="nb-frac"><span class="nb-num">3</span><span class="nb-den">12</span></span> = <span class="nb-frac"><span class="nb-num">7</span><span class="nb-den">12</span></span>' }
    },
    {
      id: 'h-mixed',
      type: 'heading',
      data: { text: 'Adding and Subtracting Mixed Numbers', level: 2 }
    },
    {
      id: 'p-mixed',
      type: 'paragraph',
      data: { text: '<strong>Method:</strong> Convert both mixed numbers to improper fractions, find a common denominator, add/subtract, then convert the result back to a mixed number.' }
    },
    {
      id: 'callout-worked-3',
      type: 'callout',
      data: { style: 'worked', title: 'Worked Example 3', text: 'Calculate 3<span class="nb-frac"><span class="nb-num">1</span><span class="nb-den">4</span></span> − 1<span class="nb-frac"><span class="nb-num">2</span><span class="nb-den">3</span></span>.\n\n<strong>Solution:</strong>\nConvert: 3<span class="nb-frac"><span class="nb-num">1</span><span class="nb-den">4</span></span> = <span class="nb-frac"><span class="nb-num">13</span><span class="nb-den">4</span></span> &nbsp;&nbsp; 1<span class="nb-frac"><span class="nb-num">2</span><span class="nb-den">3</span></span> = <span class="nb-frac"><span class="nb-num">5</span><span class="nb-den">3</span></span>\nLCM of 4 and 3 = 12.\n<span class="nb-frac"><span class="nb-num">13</span><span class="nb-den">4</span></span> = <span class="nb-frac"><span class="nb-num">39</span><span class="nb-den">12</span></span> &nbsp;&nbsp; <span class="nb-frac"><span class="nb-num">5</span><span class="nb-den">3</span></span> = <span class="nb-frac"><span class="nb-num">20</span><span class="nb-den">12</span></span>\n<span class="nb-frac"><span class="nb-num">39</span><span class="nb-den">12</span></span> − <span class="nb-frac"><span class="nb-num">20</span><span class="nb-den">12</span></span> = <span class="nb-frac"><span class="nb-num">19</span><span class="nb-den">12</span></span> = 1<span class="nb-frac"><span class="nb-num">7</span><span class="nb-den">12</span></span>' }
    },
    {
      id: 'callout-tip-1',
      type: 'callout',
      data: { style: 'tip', title: 'Exam Tip', text: 'Always simplify your final answer. If the result is an improper fraction, convert to a mixed number unless the question specifies otherwise.' }
    },
    {
      id: 'sum-1',
      type: 'summary',
      data: { text: 'Same denominator: add/subtract numerators. Different denominators: find the LCM, convert to equivalent fractions, then add/subtract. For mixed numbers, convert to improper fractions first.' }
    }
  ],
  recall: {
    enabled: true,
    cues: [
      { id: 'cue-1', blockId: 'p-same-denom', prompt: 'How do you add fractions with the same denominator?' },
      { id: 'cue-2', blockId: 'p-diff-denom', prompt: 'What must you do before adding fractions with different denominators?' },
      { id: 'cue-3', blockId: 'p-mixed', prompt: 'Describe the method for subtracting mixed numbers.' },
      { id: 'cue-4', blockId: 'callout-worked-1', prompt: 'Calculate 2/3 + 3/4. Show each step.' }
    ],
    summaryText: 'Find a common denominator to add/subtract fractions; convert mixed numbers to improper fractions first.',
    ready: true
  },
  evidence: []
};
